# Base URL of the backend API (FastAPI / Flask). When unset, the app
# automatically falls back to an in-memory mock service so the UI can be
# demonstrated without a backend.
VITE_API_URL=http://localhost:8000

# Set to "true" to force the mock service even if VITE_API_URL is reachable.
VITE_USE_MOCK_API=true
